import 'package:flutter/material.dart';

class FinderWindow extends StatelessWidget {
  const FinderWindow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
