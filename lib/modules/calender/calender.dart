import 'package:flutter/material.dart';

class CalenderWindow extends StatelessWidget {
  const CalenderWindow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Calender Window'),
    );
  }
}
