import 'package:flutter/material.dart';

class VSCodeWindow extends StatelessWidget {
  const VSCodeWindow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('VS Code Window'),
    );
  }
}
