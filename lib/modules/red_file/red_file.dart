import 'package:flutter/material.dart';

class RedFile extends StatelessWidget {
  const RedFile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Red File Window'),
    );
  }
}
